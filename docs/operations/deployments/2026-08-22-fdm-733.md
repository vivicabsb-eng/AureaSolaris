# FDM-733 exact-SHA cutover evidence

Date: 2026-08-22

## Current execution status

FDM-733 is **in progress and stopped at the production-promotion boundary**. The current upstream `main` Git object has been mirrored exactly to deployment `preview`, both Vercel preview projects are READY on that exact SHA, and the mandatory fresh FDM-732-equivalent hosted acceptance has now passed against that exact preview pair.

Production has not been promoted. Deployment mirror `main`, the canonical production deployments, and production aliases remain on the recorded pre-promotion baseline.

## Reconciled refs

- Upstream source of truth `vivicabsb-eng/AureaSolaris:main`: `d578067e3bdaedbd4f81a9ef481b6a22b856bc6d`.
- Last fully accepted FDM-732 application candidate: `33739cba13c1779e3cb5cf602ebb3a41240df724`.
- Deployment mirror `fernandodamaso/AureaSolaris-deploy:preview`: `d578067e3bdaedbd4f81a9ef481b6a22b856bc6d`.
- Deployment mirror `fernandodamaso/AureaSolaris-deploy:main`: `6ddda7627e9634e91fa303e296dec79fd93b9340`.

The accepted-candidate-to-current-main comparison is a five-commit fast-forward. Its changed paths are limited to CI/workflow fixes, verification/runbook/evidence changes, generated OpenAPI cleanup, and verification tests. No application runtime source was introduced after the accepted candidate.

## Proven exact-object preview mirror operation

The deployment mirror is an independent Git repository. Before execution, `d578067...` was not addressable in the mirror object database. A temporary import ref `fdm-733/import-d578067` was created directly at the upstream SHA through GitHub's native ref operation. GitHub accepted the exact existing Git object; fetching the commit from the deployment mirror then reported the same SHA, tree, parents, merge message, and verified signature as upstream.

Deployment mirror `preview` was then updated to `d578067...` with `force=false`. The operation was a normal fast-forward from `33739cba...`; upstream history was not rewritten.

This native exact-object import plus non-forced ref update is the proven mirror mechanism for the remaining FDM-733 ref moves. An earlier isolated control branch `fdm-733/mirror-preview-d578067` contains a guarded workflow draft (`b7023966f59a4c2874f354f97becf87385444ce7`) prepared before the native path was proven. It did not move `preview`, is not part of either deployment ref, and is not the canonical promotion mechanism.

## Exact-SHA Vercel preview pair

Both Git-connected projects detected the `preview` ref update automatically and produced READY preview deployments whose metadata reports Git ref `preview` and exact SHA `d578067e3bdaedbd4f81a9ef481b6a22b856bc6d`.

- Web project `aurea-solaris` (`prj_C1TUgYA4YskaBbxX27EYWoEV46Cr`)
  - Deployment: `dpl_CXhGnvxyFqN3M3JFsY5dJx9tyftB`
  - Immutable URL: `https://aurea-solaris-etq6gayoc-fernando-damasos-projects.vercel.app`
  - Branch alias: `https://aurea-solaris-git-preview-fernando-damasos-projects.vercel.app`
  - State: READY
  - Source: Git
- API project `aurea-solaris-api` (`prj_3H3DPEhPoX19GYyew7w1UkVgxRsS`)
  - Deployment: `dpl_CDyhw18ea3EvLs3kAKi6r1qcrbeg`
  - Immutable URL: `https://aurea-solaris-p617tlb5l-fernando-damasos-projects.vercel.app`
  - Branch alias: `https://aurea-solaris-api-git-preview-fernando-damasos-projects.vercel.app`
  - State: READY
  - Source: Git

Both build logs completed successfully. The web build emitted only the existing npm `allow-scripts` warning for `esbuild`; the API build emitted only the existing Vercel warning that project Build/Development Settings are ignored because `builds` exists in configuration.

## Fresh hosted preview acceptance — PASS

The final FDM-732-equivalent hosted gate was executed locally through the already-authenticated Vercel/Supabase provider path against the exact deployment pair above.

Accepted final results:

- exact SHA/ref/source checks for both deployments: PASS;
- stable alias binding before and after the gate: PASS;
- `scripts/verify_preview.sh`: PASS;
- private login and first-use onboarding: PASS;
- profile and birth-data persistence plus reload: PASS;
- certified natal and transit receipts rendered in the browser: PASS;
- unauthenticated boundary: `401` as required;
- cross-owner boundary: `404` as required;
- public sign-up disabled: PASS;
- no localhost, mixed-content, production API, or production Supabase traffic: PASS;
- browser console/page errors: zero;
- logout: PASS;
- `smoke_verified_preview_api.sh`: PASS, including health/readiness, unauthenticated `401`, authenticated profile, astrology calculation, and certified Swiss Ephemeris metadata.

An earlier attempt in the same audit trail failed because the browser request crossed an incompatible preview API origin and was blocked by CORS. That attempt is retained in PR/Linear history as superseded evidence. The final accepted run used the same exact web/API deployment IDs, proved stable alias binding before and after execution, and completed the full gate successfully.

## Cleanup and final provider state

The hosted gate used disposable preview-only Auth identities and temporary Vercel protection bypass values. Cleanup completed after the accepted run:

- both disposable preview Auth users deleted;
- both temporary Vercel bypass values revoked;
- final Vercel bypass counts: web `0`, API `0`;
- preview Auth users: `39`, confirmed `39`;
- production Auth users: `1`, confirmed `1`;
- Supabase environment verifier: PASS.

Preview Supabase `rosklqnnbmhowohoyboj` remains `ACTIVE_HEALTHY`, with `web_v1_core` present and owner-only RLS policies verified for `profiles`, `birth_profiles`, and `calculation_receipts`. Production Supabase `tgpcpxqqusehssaihvcp` remains `ACTIVE_HEALTHY` with the same required migration/policy posture. The only known security-advisor warning remains `auth_leaked_password_protection`, which is unchanged and plan-gated.

No password, JWT, provider key, bypass value, request payload, or private birth value is recorded in Git, Linear, or this evidence.

## Production safety / rollback baseline

Production remains on the pre-promotion baseline:

- Deployment mirror `main`: `6ddda7627e9634e91fa303e296dec79fd93b9340`.
- Prior web production deployment: `dpl_53xYnHFvT7yhx14TdiPQd5gLomgx`.
- Prior API production deployment: `dpl_GtHHFxzbWcP4M4MzjbLxuY7Wx8g6`.

No production alias, production Git ref, or production provider configuration changed during the preview phase.

Rollback is explicitly **forward-only for Git refs**. The recorded pre-promotion deployment IDs are traffic/service rollback targets; they do not authorize rewinding deployment mirror `main`. If a promoted Git state must be undone after mirror `main` advances, create an explicit forward revert commit on upstream `main`, mirror that new exact object with `force=false`, and let the Git integrations deploy it. A destructive mirror-ref rewind would require separate exceptional authorization and is not part of the normal FDM-733 rollback procedure.

## Next executable boundary

The fresh preview prerequisite is satisfied for candidate `d578067e3bdaedbd4f81a9ef481b6a22b856bc6d`. Before any production mutation, re-read upstream `main` and deployment mirror refs and stop on drift. If upstream `main` is still the accepted candidate, FDM-733 may proceed to exact-object production promotion using the proven native import/ref path, then the automatic Git-triggered redeploy proof.

PR #21 remains draft and must not be merged merely to record this evidence before production promotion, because merging it would move upstream `main` away from the SHA that just passed the hosted gate.