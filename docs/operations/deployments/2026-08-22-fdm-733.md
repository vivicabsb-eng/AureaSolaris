# FDM-733 exact-SHA cutover evidence

Date: 2026-08-22

## Current execution status

FDM-733 is **in progress and stopped on the production API health gate**.

The exact current upstream `main` candidate `d578067e3bdaedbd4f81a9ef481b6a22b856bc6d` passed the mandatory fresh hosted preview acceptance. Deployment mirror `main` was then fast-forwarded to that exact object with `force=false`, and both Vercel projects automatically created Git-sourced production deployments at the same SHA.

The web production deployment is READY and the canonical frontend returns HTTP 200. The API production deployment is also READY at the Vercel deployment layer, but canonical `/health` returns HTTP 500 `FUNCTION_INVOCATION_FAILED`. Runtime logs show import-time Settings validation rejects the production `AUREA_DATABASE_URL` because it does not use the required dedicated production TLS role. The automatic-redeploy proof therefore has **not** started, and FDM-734 remains blocked.

## Reconciled refs

- Upstream source of truth `vivicabsb-eng/AureaSolaris:main`: `d578067e3bdaedbd4f81a9ef481b6a22b856bc6d`.
- Last fully accepted FDM-732 application candidate: `33739cba13c1779e3cb5cf602ebb3a41240df724`.
- Deployment mirror `fernandodamaso/AureaSolaris-deploy:preview`: `d578067e3bdaedbd4f81a9ef481b6a22b856bc6d`.
- Deployment mirror `fernandodamaso/AureaSolaris-deploy:main`: `d578067e3bdaedbd4f81a9ef481b6a22b856bc6d` after the production promotion attempt.

The accepted-candidate-to-current-main comparison is expected repository history; no application runtime source was introduced after the accepted FDM-732 application candidate.

## Proven exact-object mirror mechanism

The deployment mirror is an independent Git repository. The exact upstream object was made addressable in the mirror through a temporary native GitHub ref, its SHA/tree/parents were verified, and mirror refs were advanced with `force=false`.

This native exact-object import plus non-forced ref update is the canonical FDM-733 mirror mechanism. The earlier isolated workflow draft on `fdm-733/mirror-preview-d578067` did not perform the live ref move and remains historical only.

## Fresh hosted preview acceptance — PASS

Exact preview deployments for `d578067e3bdaedbd4f81a9ef481b6a22b856bc6d`:

- Web: `dpl_CXhGnvxyFqN3M3JFsY5dJx9tyftB`.
- API: `dpl_CDyhw18ea3EvLs3kAKi6r1qcrbeg`.

Final accepted results:

- exact SHA/ref/source checks: PASS;
- stable alias binding before/after: PASS;
- `scripts/verify_preview.sh`: PASS;
- private login/onboarding: PASS;
- profile and birth persistence/reload: PASS;
- certified natal and transit receipts rendered: PASS;
- unauthenticated `401`: PASS;
- cross-owner `404`: PASS;
- public sign-up disabled: PASS;
- no localhost, mixed-content, production API, or production Supabase traffic: PASS;
- browser console/page errors: zero;
- logout: PASS;
- `smoke_verified_preview_api.sh`: PASS, including health/readiness and certified Swiss Ephemeris calculation.

Cleanup after the accepted preview run also passed: disposable Auth identities were deleted, temporary Vercel bypass values were revoked, bypass counts returned to zero, preview Auth returned to `39/39` confirmed, and production Auth remained `1/1` confirmed.

## Production promotion attempt

Pre-promotion rollback baseline:

- Mirror `main`: `6ddda7627e9634e91fa303e296dec79fd93b9340`.
- Prior web production deployment: `dpl_53xYnHFvT7yhx14TdiPQd5gLomgx`.
- Prior API production deployment: `dpl_GtHHFxzbWcP4M4MzjbLxuY7Wx8g6`.

Mirror `main` was advanced from `6ddda762...` to accepted SHA `d578067...` with `force=false`. Vercel Git integration reacted automatically and created:

- API production: `dpl_71iojdUpB3gM9WQzgUXFycWDG7vL` — source `git`, ref `main`, SHA `d578067...`, READY at the deployment layer.
- Web production: `dpl_4E7jHm8jDCE3NVbQn8hYgngekN7L` — source `git`, ref `main`, SHA `d578067...`, READY.

The API build log confirms Vercel cloned `fernandodamaso/AureaSolaris-deploy`, branch `main`, commit `d578067`.

Canonical frontend `https://aurea-solaris.vercel.app/` returns HTTP 200.

Canonical API `https://aurea-solaris-api.vercel.app/health` returns HTTP 500 with `FUNCTION_INVOCATION_FAILED`. Vercel runtime logs show:

- `api/index.py` fails during application import;
- `Settings.from_env(...)` raises a Pydantic validation error;
- reason: `database URL must use the deployed environment's dedicated TLS role`.

The code requires deployed database URLs to use the Supabase pooler host on port 5432, database `/postgres`, TLS (`sslmode=require|verify-ca|verify-full`), and username `aurea_api.<environment-supabase-ref>`. For production the expected ref is `tgpcpxqqusehssaihvcp`.

No secret value was read or recorded.

## Production provider state

Production Supabase `tgpcpxqqusehssaihvcp` was rechecked after the failure:

- project: `ACTIVE_HEALTHY`;
- Auth users: `1`, confirmed `1`;
- `web_v1_core` migration present;
- `profiles`, `birth_profiles`, and `calculation_receipts` have RLS enabled;
- expected owner-only policies remain present with `auth.uid() = user_id` semantics.

This isolates the failure to production API environment configuration rather than Supabase project health/schema state.

## Rollback and stop boundary

Normal Git history remains forward-only. The recorded prior Vercel production deployments are service/traffic rollback targets; they do not authorize rewinding mirror `main`. A destructive mirror-ref rewind would require separate exceptional authorization.

The connected Vercel toolset available in this execution environment exposes deployment inspection/logs but no production alias rollback or environment-variable mutation action. No alternate installable plugin exposing that capability was found.

Therefore:

- do not start the automatic-redeploy proof;
- do not mark FDM-733 Done;
- do not begin FDM-734 destructive cleanup;
- repair production `AUREA_DATABASE_URL` through an approved authenticated Vercel environment-management path (without exposing the value), or perform an authorized service rollback;
- after repair, require canonical API `/health` and `/ready` to pass, then resume the remaining FDM-733 sequence.

PR #21 remains draft while this production gate is unresolved.
