# FDM-733 exact-SHA cutover evidence

Date: 2026-08-22

## Current execution status

FDM-733 is **in progress and intentionally stopped before production promotion**. The current upstream `main` Git object has been mirrored exactly to deployment `preview`, both Vercel preview projects are READY on that exact SHA, and fresh provider/RLS checks are green. The mandatory credentialed FDM-732-equivalent hosted ownership gate has not been rerun because the prior process-only preview credentials and Vercel automation bypass values were deliberately removed after FDM-732 and no retained approved secure credential source is available through the connected execution tools.

Production was not modified and no acceptance requirement was weakened or carried forward as a substitute for the required fresh gate.

## Reconciled refs

- Upstream source of truth `vivicabsb-eng/AureaSolaris:main`: `d578067e3bdaedbd4f81a9ef481b6a22b856bc6d`.
- Last fully accepted FDM-732 application candidate: `33739cba13c1779e3cb5cf602ebb3a41240df724`.
- Deployment mirror `fernandodamaso/AureaSolaris-deploy:preview`: `d578067e3bdaedbd4f81a9ef481b6a22b856bc6d`.
- Deployment mirror `fernandodamaso/AureaSolaris-deploy:main`: `6ddda7627e9634e91fa303e296dec79fd93b9340`.

The accepted-candidate-to-current-main comparison is a five-commit fast-forward. Its changed paths are limited to CI/workflow fixes, verification/runbook/evidence changes, generated OpenAPI cleanup, and verification tests. No application runtime source was introduced after the accepted candidate.

## Exact-object preview mirror operation

The deployment mirror is an independent Git repository. Before execution, `d578067...` was not addressable in the mirror object database. A temporary import ref `fdm-733/import-d578067` was created directly at the upstream SHA through GitHub's native ref API. GitHub accepted the exact existing Git object; fetching the commit from the deployment mirror then reported the same SHA, tree, parents, merge message, and verified signature as upstream.

Deployment mirror `preview` was then updated to `d578067...` with `force=false`. The operation was a normal fast-forward from `33739cba...`; upstream history was not rewritten.

An earlier isolated control branch `fdm-733/mirror-preview-d578067` contains a guarded workflow draft (`b7023966f59a4c2874f354f97becf87385444ce7`) prepared before the native exact-object import path was proven. It did not move `preview` and is not part of either deployment ref.

## Exact-SHA Vercel preview pair

Both Git-connected projects detected the `preview` ref update automatically and produced READY preview deployments whose metadata reports Git ref `preview` and exact SHA `d578067e3bdaedbd4f81a9ef481b6a22b856bc6d`.

- Web project `aurea-solaris` (`prj_C1TUgYA4YskaBbxX27EYWoEV46Cr`)
  - Deployment: `dpl_CXhGnvxyFqN3M3JFsY5dJx9tyftB`
  - Immutable URL: `https://aurea-solaris-etq6gayoc-fernando-damasos-projects.vercel.app`
  - Branch alias: `https://aurea-solaris-git-preview-fernando-damasos-projects.vercel.app`
  - State: READY
  - Source: Git
- API project `aurea-solaris-api` (`prj_3H3DPEhPoX19GYyew7w1UkVgxRsS`)
  - Deployment: `dpl_CDyhw18ea3EvLs3kAKi6r1qcrbeg`
  - Immutable URL: `https://aurea-solaris-p617tlb5l-fernando-damasos-projects.vercel.app`
  - Branch alias: `https://aurea-solaris-api-git-preview-fernando-damasos-projects.vercel.app`
  - State: READY
  - Source: Git

Both build logs completed successfully. The web build emitted only the existing npm `allow-scripts` warning for `esbuild`; the API build emitted only the existing Vercel warning that project Build/Development Settings are ignored because `builds` exists in configuration.

## Fresh Supabase/provider checks

Preview Supabase `rosklqnnbmhowohoyboj`:

- project: `ACTIVE_HEALTHY`;
- remote migration: `web_v1_core` present (`20260821172829` execution version);
- Auth users: `39`, confirmed: `39`;
- `profiles`, `birth_profiles`, and `calculation_receipts`: RLS enabled;
- exact owner policies: `profiles_owner_all`, `birth_profiles_owner_all`, `calculation_receipts_owner_all`;
- policies apply to `{authenticated}`, command `ALL`, with both `USING` and `WITH CHECK` equal to `(( SELECT auth.uid() AS uid) = user_id)`;
- security advisors: only the previously accepted `auth_leaked_password_protection` warning.

Production Supabase `tgpcpxqqusehssaihvcp`:

- project: `ACTIVE_HEALTHY`;
- remote migration: `web_v1_core` present (`20260821172901` execution version);
- Auth users: `1`, confirmed: `1`;
- the same three tables have RLS enabled with the same exact owner-only policies;
- security advisors: only the previously accepted `auth_leaked_password_protection` warning.

No Supabase schema, Auth identity, or provider configuration was changed during this execution.

## Mandatory preview gate not yet satisfied

The repository's accepted `scripts/verify_preview.sh` requires process-only preview credentials and protection values to prove:

- private login and first-use onboarding;
- persisted profile/birth data and reload;
- certified natal and transit receipts rendered in the browser;
- unauthenticated `401` and cross-owner `404` boundaries;
- public sign-up disabled through the live Auth endpoint;
- API `/health` and `/ready` behavior behind preview protection;
- no localhost, mixed-content, production API, or production Supabase traffic;
- no browser console/page errors;
- cleanup of temporary preview identities and protection values.

The final FDM-732 record explicitly states that those credentials were supplied through process-only secure variables and that two temporary preview users plus two local-only Vercel automation bypass values were removed after the accepted run. The currently connected Vercel/Supabase/GitHub tools expose neither the removed bypass-management capability nor a retained secure preview email/password/second-user JWT source. No password, JWT, service key, bypass value, or private birth payload was requested in chat, written to Git, or stored in Linear.

Because FDM-733 explicitly requires a **fresh** credentialed hosted acceptance for `d578067...`, the prior FDM-732 runtime evidence is not treated as sufficient promotion evidence even though the post-candidate delta contains no application runtime source change.

## Production safety / rollback baseline

Production remains on the pre-execution baseline:

- Deployment mirror `main`: `6ddda7627e9634e91fa303e296dec79fd93b9340`.
- Prior web production deployment: `dpl_53xYnHFvT7yhx14TdiPQd5gLomgx`.
- Prior API production deployment: `dpl_GtHHFxzbWcP4M4MzjbLxuY7Wx8g6`.

No production alias, production Git ref, or production provider configuration changed. The automatic production-redeploy proof has not started because its prerequisite production promotion is intentionally blocked by the incomplete fresh preview gate.

## Next executable boundary

Resume only when an approved secure execution environment can supply the preview-only values required by `scripts/verify_preview.sh` without exposing them. Re-run the complete hosted gate against the exact READY pair above. If it passes and upstream `main` is still `d578067...`, FDM-733 may proceed to the guarded exact-object production promotion. If any check fails or upstream moves, stop and establish a new exact preview candidate before production.
