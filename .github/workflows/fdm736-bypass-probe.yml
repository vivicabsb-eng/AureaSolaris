name: FDM-736 Protected Preview Audit

on:
  pull_request:

permissions:
  contents: read

jobs:
  retired:
    if: github.head_ref == 'fdm-736/hosted-e2e-runner-v2'
    runs-on: ubuntu-latest
    steps:
      - run: echo 'FDM-736 disposable hosted audit runner retired after successful audit.'
